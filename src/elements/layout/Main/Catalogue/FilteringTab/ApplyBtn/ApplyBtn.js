import { Button } from "react-bootstrap";

function ApplyBtn() {
    return (
        <Button variant="outline-primary" className='width-form align-self-center'>Apply</Button>
    );
}

export default ApplyBtn;